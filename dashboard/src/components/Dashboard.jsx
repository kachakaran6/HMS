import React from "react";

const Dashboard = () => {
  //      if (!isAuthenticated) {
  //     return <Navigate to="/login" />;
  //   }
  return <div>Dashboard</div>;
};

export default Dashboard;
